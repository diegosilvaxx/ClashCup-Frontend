import RankingGrid from '../../pages/cadastros/ranking/container/ranking.grid';

const ranking = [
  {
    path: '/ranking',
    component: RankingGrid,
    isPrivate: true,
    exact: true,
    breadcrumb: 'Clash Cup / Ranking',
  },
];

export default ranking;
